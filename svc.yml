apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: deployment
  name: deployment
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: deployment
  type: LoadBalancer
status:
  loadBalancer: {}
